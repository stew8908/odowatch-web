{
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/public",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./web ./api",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

